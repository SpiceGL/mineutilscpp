20250930
1、需要更新Timepoint转某个单位的总时间量，比如把时间点转为总秒数。
    × 决定将Timepoint分离为系统时间和Steady时间
    
2、DateTime考虑增加毫秒微秒等成员，即使转换为日期后不显示。
    × 添加了表示时间戳的成员后，毫秒微秒等成员必要性较小。
    
3、MinTimeGuard(long long target_time)的注释有错要修正。
    √ 已修正
    
4、ArgumentParser可以添加一个检查是否缺少给定参数的功能。
    × 感觉不是很必要，这个功能可以由使用者自己简单判断，由库实现感觉违背了轻量化原则
    √ 但是在parser方法中添加了值选项为空时正常解析但返回正数的设定
    
5、extra里可以考虑加一些ffmpeg的封装。

6、MinTimeGuard可以考虑增加一个提前终止的接口，避免等待时间较长时遇到突发事件无法结束。
    √ 添加了一个谓词，用于让用户传入提前结束条件

7、print、toStr和format函数，到底是保持现在这样通过SFINAE特性接受任意类型，还是改为通过类型检查拒绝不支持operator<<的类型？
    × 保持现在的样子吧，不然有多个参数时，编译错误又得查是哪个参数不支持，而且不这样会不敢用，总担心不能输出

8、ArgumentParser::getValueOpt应该改回const string&返回，返回指针感觉不合理，没有什么意义。
    √ 更改为引用了，之前返回指针好像确实没什么意义。

9、遇到一种情况，对象A中注册另一个对象B的回调，使用了对象A的this，但无法保证回调在对象A析构之前结束。这种时候需要一个哨兵，让回调可以锁住对象A的析构过程，保证回调内判断哨兵时和使用对象A的this时，对象A不会被析构。
    × 也没必要实现，完全可以由类实现两个shared_ptr的参数，mutex和bool，再传入回调，就保证了回调执行时不会析构，析构后bool值还有效可以判断对象是否析构

10、时间库可以扩展一个秒数与"2025-10-25 12:30:15"互转的功能
    o 考虑放在extra里面，因为这个功能在标准库范围内，难以判断非法时间转秒数的过程是否成功，导致错误码无法决定性判断操作是否成功

11、时间库TimePoint要不还是分成steady和system吧，现在合在一起的方案，难以对用户自行输入的时间值进行计算。
    × 现在内部使用数值来保存，分开的必要性不强了，而且增加了用户输入时间值的接口